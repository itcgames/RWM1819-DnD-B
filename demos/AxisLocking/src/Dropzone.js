/**
 * basic entity to represent a dropzone
 */
class DropZone {
    constructor(entity) {
      this.entity = entity;
    }

    // validation
    validDrop(bool) {
      //this.entity.processDrop(bool);
    }
  }